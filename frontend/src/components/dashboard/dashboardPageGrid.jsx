const DashboardPageGrid = () => {

    // Hardcoded data (temp before dynamic database integration)

    const tempDatabase = [
        {
            title: "RecordOne",

        }
    ]

    return (
        <div>

        </div>
    );
};

export default DashboardPageGrid;